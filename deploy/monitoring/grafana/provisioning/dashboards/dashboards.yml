apiVersion: 1

providers:
  - name: Blocknet Pool
    orgId: 1
    folder: Blocknet Pool
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards/blocknet-pool
